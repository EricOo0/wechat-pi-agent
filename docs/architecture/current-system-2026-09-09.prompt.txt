Create a polished precise Chinese software system architecture diagram, wide landscape 2400x1600 or similar, crisp large Chinese typography, white background, navy text, blue/teal/purple accents, flat rounded rectangles, orthogonal arrows, generous whitespace. Title "WeChat × Pi Agent｜系统架构" subtitle "当前代码实现 · 单实例 Node.js / TypeScript · 2026-09-09". Not proposed future architecture. No decorative illustrations.
Top main business flow across width, numbered 1 to 6: "微信用户" → "iLink 接入\nPollLoop · ILinkHttpClient\n长轮询 / 白名单 / 消息去重" → "持久任务队列\nSQLite Inbox / Session / Turn" → "任务执行\nTurnWorkerLoop → RunNextTurn\n命令路由 / 文件保存 / Agent 调用" → "回复队列\nSQLite Outbox" → "回复投递\nOutboxWorkerLoop → DeliverReply\n分段文本 / 失败重试". Return arrow from last to 微信用户 along outside top labeled "微信文本回复". Broad enclosing service boundary excludes 微信用户. Main flow completion RunNextTurn -> Outbox; Agent branch runs down from task execution and returns result to task execution.

Middle central dominant card "PiAgentGateway · Agent 运行时" contains three stacked sublayers "pi-coding-agent：会话 / 上下文压缩 / Skills", "pi-agent-core：模型 ↔ 工具循环", "pi-ai：Provider 协议 / 模型流". Arrow right to external card "模型服务\n当前默认 openai-codex\n其他 Provider 由 Pi 配置与认证". External model card outside service boundary.
Middle left card "模型与认证管理" text "微信命令 / Admin → ModelManagement", "认证后列模型 · 按 subjectKey 保存选择", "TurnBinding 固定本轮模型", "ProviderRequestGate 协调请求与认证". Dashed link into Agent runtime. Small note "管理入口受 MODEL_MANAGEMENT_ENABLED 控制".
Lower row three capability cards connected to Agent runtime:
1 "受控工具执行" with sequence "PermissionService → PolicyCompiler", then nested distinctly shaded subprocess boundary "LocalSandboxExecutor\nSeatbelt（macOS）/ bubblewrap（Linux）", text "read / list_files / sandbox_write / http_get / bash", note "确认授权后去重续跑；Full Access 使用当前 OS 用户权限".
2 "用户文件与上下文" text "图片 → Pi ImageContent", "PDF 原件 → 用户文件库", "file_list / file_use → Codex 文件输入", "纯文件上传：保存并记录回执，无需调用模型".
3 "会话与用户记忆" text "EndSession → memory_jobs", "MemoryWorkerLoop → PiMemoryGenerator", "提炼明细 / 合并总览", "新会话加载快照 · memory_search / memory_read". Small dashed arrow from memory card to model services labeled "独立模型调用".
Bottom horizontal storage band "持久化" three columns: "app.db｜消息 / 会话 / 任务 / Outbox / Trace / 文件索引 / 模型选择与绑定"; "permissions.db｜授权 / 申请 / 审计"; "本地文件｜Pi JSONL 会话 / PDF 与图片 / Markdown 记忆 / 认证配置". Use a few dashed connectors from components to storage, not overwhelming spaghetti.
Narrow side or top service support strip "Bootstrap：配置 / 依赖组装 / 启停与恢复" and "Admin：Trace / Token / 健康 / 指标 / 模型管理". They are supporting components, not sequential nodes of message flow.
Legend footer "实线：业务流与调用   虚线：配置、查询与存储依赖" and "模型与微信请求在主进程；普通工具子进程受 OS 沙箱约束". Ensure arrow clarity and readable legible text, avoid crossings and excessively tiny text. Do not depict microservices, Redis, vector databases, embedding, Docker or automatic interrupted tool replay. Main application single process and separate tool subprocess indicated accurately.
