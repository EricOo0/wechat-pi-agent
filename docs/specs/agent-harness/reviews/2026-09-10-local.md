# H-001 本地完成核对

stage: completion；implementation_ready: true；范围为 H-001 首期全部代码及必要本地检查，不包含部署或真实账号验收。

基线：`77dfbf431a75631cd53b4a50538abe60c5909187`。核对未提交工作区，未暂存；暂存指纹不适用，本报告不冒充 pre-commit 隔离快照核对。后续提交仍由 Hook 针对实际暂存版本生成报告。

实际执行 `npm run check`，退出 0：146 tests / 44 files，架构检查、Lint、类型检查和构建通过。[原始输出及文件 SHA256](../evidence/README.md)对应本次代码版本，测试使用临时数据库和合成 Provider。

| 条款 | 状态 | 实现依据 | 已运行验证（test/integration 下） | 核对范围 |
|---|---|---|---|---|
| H-001-1 | conforms | TaskStore.attach / input-policy | tasks.test.ts:50,170 | local：普通输入、控制与重复投递 |
| H-001-2 | conforms | TaskManager.execute / TaskStore.runs | tasks.test.ts:92,209 | local：内部多次运行与查询 |
| H-001-3 | conforms | TaskStore.control / TaskManager.handleMessage | tasks.test.ts:137,170 | local：即时暂停、输入保留及控制去重 |
| H-001-4 | conforms | SqliteControlPlane.endSession / 启动恢复 | tasks.test.ts:192 | local：历史保留、未完成取消 |
| H-001-5 | conforms | parseTaskOutcome / TaskManager.execute | tasks.test.ts:50,92,119,203 | local：严格 JSON 与完成申请门槛 |
| H-001-6 | conforms | TaskStore.enqueue / IngestMessage | tasks.test.ts:92,149 | local：内部独立 Turn，共享真实来源 |
| H-001-7 | conforms | attach / begin / settle 的 revision 校验 | tasks.test.ts:128,137,181 | local：迟到 Review、新输入和会话边界 |
| H-001-8 | conforms | waiting 分支 / wakePermission | tasks.test.ts:77,119,149 | local：等待零忙循环、文件答复和权限续跑 |
| H-001-9 | conforms | PiAgentGateway.beforeModelCall / reserveRound | tasks.test.ts:101,112；model-switching.test.ts | local：30 轮原子预算与 SDK 实际调用边界 |
| H-001-10 | conforms | completeTurn / settle / turnFailed | tasks.test.ts:237,170 | local：Outbox 注入失败回滚与控制幂等 |
| H-001-11 | conforms | RunNextTurn / Memory source / Admin Trace / endSession | tasks.test.ts:59,77,162,181,224；完整既有回归 | local：文件、会话、记忆和其他能力回归 |
| H-001-12 | conforms | continueOrPause / beginReview / budget control | tasks.test.ts:101,112 | local：第 30 轮 Review、禁止第 31 轮、显式追加 |
| H-001-13 | conforms | beginReview / recordReview / PiTaskReviewer | tasks.test.ts:92,101,119；task-reviewer.test.ts | local：独立绑定与计数、去重、缺口或等待 |

实现入口：[Task 模块](../../../../src/modules/tasks/index.ts)、[Task Manager](../../../../src/modules/tasks/application/task-manager.ts)、[Task Store](../../../../src/adapters/sqlite/sqlite-task-store.ts)、[Pi Gateway](../../../../src/adapters/pi/pi-agent-gateway.ts)、[Review](../../../../src/adapters/pi/pi-task-reviewer.ts)、[事务](../../../../src/adapters/sqlite/sqlite-control-plane.ts)。

测试入口：[Task 集成](../../../../test/integration/tasks.test.ts)、[迁移](../../../../test/integration/task-migration.test.ts)、[Review SDK](../../../../test/integration/task-reviewer.test.ts)、[模型 SDK](../../../../test/integration/model-switching.test.ts)。migration 9 额外验证 v8 旧数据 ID/rowid/外键关系、内部请求与消息去重同时成立。

关联 R-001-02/03/06/11/13：模块公开边界、普通流程、事务、生命周期保留，架构与原有回归通过。M-001-3/5：新 Run 和 Review 分别绑定当前模型，既有可信 owner/认证 gate 保留，模型回归及合成 Provider 测试通过。F-001 附件兼容：已有文件集成与新增文件 HITL 测试通过；未宣称真实 PDF 模型验收。

## 外部待验收

H-001-11、H-001-5/13 的真实微信输入投递、真实 OAuth/API Key、真实模型 JSON 协议遵循/Review 质量、PDF 实际推理与 Linux 沙箱：unverified，verification_scope=external。未部署、未重启服务、未调用真实账户。该缺口不作为本地已实现功能的线上证明，lifecycle 保持 planned、effective=null。

## 决策与偏差核对

最终结构化 JSON、最新未关闭 Task、单会话单未关闭任务、每次 Run/Review 绑定模型、失败调用计入预算、明确追加轮数与重启历史只读均有用户明确确认。没有引入 Graph/checkpoint 或自动恢复。未发现需要用户新决策的本地 missing/deviates。

物理存储将目标约束表达为初始 goal + 有序 inputs/replyTo，不以短答复覆盖目标；上传已有目标的文件答复继续，未知目标上传等待。Review 无工具，只核对提供的证据；这不能保证答案正确，真实质量待验收。

[本次核对的规格指纹](2026-09-10-local-fingerprints.json)。
