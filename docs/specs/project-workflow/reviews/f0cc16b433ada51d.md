# 规格与实现核对

代码基线：13b1d7e12bb2586ce5ee59b8ff1ab081f0a1865c
暂存及规则指纹：f0cc16b433ada51d8fecaf877b42a1afb64e83dceb781018de0e64873d83a283
阶段：completion

本次 changelog 明确声明“提交核对超时延长”已完成：project-spec-review 与 project-change-sync 两阶段的 codex exec 超时从 180 秒调整为 600 秒，并声明回归通过。受影响现行规格为 W-001；核对范围覆盖共享调用实现、两个必要调用方、超时终止与失败阻断回归、操作文档及版本化证据。

规格：docs/specs/project-workflow/spec.md
规格 SHA256：2c61796775573a761019ce435fbaf65b89c1df89d79eb58d5cb8f0ed702fee34
实现就绪：True

| 条款 | 结论 | 代码依据 | 验证依据 | 原因及处置 |
|---|---|---|---|---|
| W-001-6：失败或超时不得放行，超时后应终止子进程且不泄露原始错误日志。 | conforms | scripts/project-workflow/workflow.py:222-254；communicate 超时为 600 秒，超时后依次 SIGTERM、等待 5 秒、必要时 SIGKILL，最终抛出不含原始 stderr 的固定错误。 | local: test/project-workflow/test_workflow.py:123-150；版本化结果 docs/specs/project-workflow/evidence/timeout-600-tests.txt:54-56 记录 Ran 30 tests / OK；timeout-600-tests.json 中 workflow.py 与 test_workflow.py 的 SHA256 分别为 037a20c5… 和 6e8e309f…，与当前快照实算一致。 | 实现保持失败阻断与进程组清理，并将实际 communicate 参数及错误文本统一更新为 600 秒；回归明确断言 600 秒参数、SIGTERM 清理和错误信息。只读核对采用已有且版本匹配的测试证据，未重新运行测试。 |
| W-001 Git pre-commit 第 2 条：先执行 project-spec-review，再执行 project-change-sync，且每阶段最多 600 秒。 | conforms | scripts/project-workflow/workflow.py:222-258,469-476；spec_review 和后置同步均调用同一 review 函数，review 唯一模型调用超时为 600 秒；pre_commit 保持先核对、后同步的调用顺序。 | local: test/project-workflow/test_workflow.py:136-150 验证共享 review 的 communicate timeout=600；docs/specs/project-workflow/evidence/timeout-600-tests.json 与当前代码、测试哈希匹配。 | 两个模型阶段共用已调整的 review 实现，因此均受 600 秒上限约束；调用顺序未改变。 |
| W-001-7：前置核对失败时不调用同步；既有缓存、暂存保护和失败阻断约束不得因超时调整而退化。 | conforms | scripts/project-workflow/workflow.py:441-480；核对调用位于同步调用之前，异常直接退出；blockers 在同步前抛错；缓存、规则一致性、暂存快照及应用前检查路径未被本次 diff 修改。 | local: test/project-workflow/test_workflow.py:123-129 覆盖失败后不缓存；同文件完整版本由 timeout-600-tests.json 的匹配 SHA256 绑定，timeout-600-tests.txt 记录当前版本 30 项测试通过。 | 本次改动仅调整共享超时参数、错误文本和进度提示；必要的失败路径与保护逻辑仍在，并由版本匹配的完整测试文件及测试结果覆盖。 |
| W-001 的操作说明与现行规格应准确反映 600 秒单阶段上限，不把实现完成表述为上线。 | conforms | docs/specs/project-workflow/spec.md:53-57；docs/runbook/project-workflow.md:15-21；docs/changelog/2026-09.md:3-8。 | local: 只读逐文件核对：spec 与 runbook 均写明每阶段 600 秒、两阶段约 20 分钟；changelog 链接版本化证据并明确“未提交或推送”。相关证据链接目标文件存在。 | 规格、操作说明、交付记录与代码一致，未声称发布或上线，也未通过修改要求掩盖实现偏差。changelog 所称 30 项测试由证据支持；本次未独立重跑其所称文档检查。 |

这是对应暂存版本的静态核对记录；不等同运行测试或上线验收。
