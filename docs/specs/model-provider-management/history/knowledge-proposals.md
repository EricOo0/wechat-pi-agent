> 历史材料：不作为现行规格。请以 [spec](../spec.md) 为准；勾选和状态仅表示原记录时点。

# 项目知识建议（未应用）

实现验收后建议更新 README 的命令与模型管理章节：

- 用户默认模型与Turn实际模型是不同状态，Trace以实际调用为准。
- /model针对当前供应商，跨供应商选择使用完整/provider命令。
- 凭证变更与模型选择分开，共享Pi凭证需等待该provider活动任务退出。
- 未识别命令原文传给Agent，帮助仅明确触发。

依据见design.md第10节。以上是待实现约定，不能提前写为现有能力；未更新长期记忆。
