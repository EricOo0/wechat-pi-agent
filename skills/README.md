# 工程内置 Skills

在此目录下为每项能力创建 `<name>/SKILL.md`，随工程一起发布。文件头必须包含非空 `description`，建议明确填写唯一 `name`。参考资料和脚本放在该 Skill 子目录内。

此目录由 `WORKSPACE_ROOT/skills` 定位，受 `PI_LOAD_LOCAL_SKILLS` 控制；加载和优先级说明见工程 README。此说明文件不含 Skill 元信息，不会成为可用 Skill。
