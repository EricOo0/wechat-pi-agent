NODE_ENV=development
WORKSPACE_ROOT=.
DATA_DIR=./data
# Empty: create a persistent random ID in DATA_DIR/executor-id.
# Use a different ID for another execution environment.
PERMISSION_EXECUTOR_ID=
ADMIN_HOST=127.0.0.1
ADMIN_PORT=9464
LOG_LEVEL=info
TRACE_RETENTION=100
SYSTEM_PROMPT_PATH=./src/prompts/wechat-assistant.md

# Pi SDK: PI_MODEL_ID may be empty; first live startup opens OAuth and asks you to select.
PI_PROVIDER=openai-codex
PI_MODEL_ID=
PI_THINKING_LEVEL=medium
PI_AUTH_PATH=~/.pi/agent/auth.json
PI_MODELS_STORE_PATH=~/.pi/agent/models.json
PI_LOAD_LOCAL_SKILLS=false

# Each authenticated user gets a subdirectory and a persistent permission policy.
TOOL_SANDBOX_ROOT=./data/tool-workspace
# Legacy flags below are ignored; use conversational permission requests instead.
TOOL_HTTP_ENABLED=false
TOOL_HTTP_ALLOWED_HOSTS=
TOOL_SHELL_ENABLED=false

# iLink: missing credentials trigger the interactive QR flow.
ILINK_BASE_URL=https://ilinkai.weixin.qq.com
ILINK_CDN_BASE_URL=https://novac2c.cdn.weixin.qq.com/c2c
ILINK_BOT_TOKEN=
ILINK_BOT_ID=
ILINK_USER_ID=
ILINK_ALLOWED_SENDER_ID=
ILINK_AUTO_LOGIN=false

# Safe default for first startup: no external calls, deterministic echo responses
# Set false only after configuring both Pi and iLink credentials below.
DRY_RUN=true
