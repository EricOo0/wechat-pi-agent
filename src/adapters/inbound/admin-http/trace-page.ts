export const TRACE_PAGE_HTML = String.raw`<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Agent Traces</title>
<style>
:root{color-scheme:light;--bg:#f7f8fc;--line:#e5e8f0;--ink:#202b40;--muted:#758096;--accent:#6551ea}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px/1.6 system-ui,-apple-system,sans-serif}button,input{font:inherit}button{background:white;border:1px solid var(--line);border-radius:7px;padding:6px 11px;cursor:pointer;color:inherit}button:hover,button.active{background:#efecff;color:var(--accent)}button:focus-visible,input:focus-visible{outline:2px solid var(--accent)}header{height:72px;display:flex;align-items:center;gap:14px;padding:14px 24px;border-bottom:1px solid var(--line);background:white}header strong{font-size:23px}input{border:1px solid var(--line);border-radius:7px;padding:8px;min-width:0}header input[type=search]{margin-left:auto;width:300px}main{display:grid;grid-template-columns:220px 330px minmax(420px,1fr);height:calc(100vh - 72px)}aside,#tree,#detail{overflow:auto;padding:18px;border-right:1px solid var(--line)}aside,#detail{background:white}.muted{font-size:12px;color:var(--muted)}.notice{background:#f4f5fa;border-radius:7px;padding:10px 12px;color:var(--muted);font-size:12px;margin:12px 0}.session{display:block;text-align:left;width:100%;margin:7px 0;padding:12px;border-color:transparent;background:transparent}.session.active{border-color:#dcd5ff;background:#f1edff}.ellipsis{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.session strong{display:block}.toolbar,.tabs,.meta{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.toolbar{margin:12px 0}.tabs{border-bottom:1px solid var(--line);padding:12px 0;margin-bottom:18px}.tabs button{border:0}.tabs button.active{box-shadow:0 2px var(--accent);border-radius:4px 4px 0 0}.turn{margin:14px 0;border:1px solid var(--line);border-radius:8px;background:white;overflow:hidden}.turn-head{display:block;width:100%;text-align:left;border:0;border-radius:0;background:#fbfbff;padding:12px}.turn-head strong{display:block}.nodes{padding:5px 6px 10px}.node{display:flex;align-items:center;gap:7px;width:100%;text-align:left;border:0;background:transparent;margin:2px 0;font-size:12px;padding:8px;border-radius:5px}.node .label{flex:1;min-width:0}.node.selected{background:#eeeaff;color:var(--accent)}.children{margin-left:13px;padding-left:8px;border-left:1px solid #d8d0fa}.badge{display:inline-block;font-size:11px;padding:1px 6px;border:1px solid currentColor;border-radius:5px;white-space:nowrap}.succeeded{color:#188254}.failed{color:#cb3656}.running{color:#ac710f}.incomplete{color:#758096}.icon{width:22px;height:22px;border-radius:5px;background:#efeaff;color:var(--accent);text-align:center;flex:none}.model-icon{background:#e4f5f1;color:#128575}.tool-icon{background:#e5f4ed;color:#248653}h1{font-size:23px;margin:0 0 7px;overflow-wrap:anywhere}h2{font-size:17px;margin:24px 0 12px}h3{font-size:13px;margin:0 0 8px}.card{border:1px solid var(--line);border-radius:8px;margin:12px 0;overflow:hidden}.card-title{background:#f6f7fb;padding:10px 14px;font-weight:600;color:#63718b}.card-body{padding:14px}.prose,pre{white-space:pre-wrap;overflow-wrap:anywhere;word-break:break-word}pre{font:12px/1.7 ui-monospace,SFMono-Regular,monospace;margin:0}.reasoning{background:#faf8ff;border-left:3px solid #b9a5f2;padding:12px;margin-bottom:12px}.block+.block{margin-top:14px}.pill{display:inline-block;background:#f0ecfa;color:#776094;border-radius:5px;padding:2px 7px;font-size:11px;margin-bottom:7px}details{margin:10px 0;border:1px solid var(--line);border-radius:7px;padding:11px}summary{cursor:pointer;color:#68748a}.meta{font-size:12px;color:var(--muted);margin:8px 0}.chat-item{padding:12px;margin:10px 0;background:white;border:1px solid var(--line);border-radius:8px}.empty{padding:30px;color:var(--muted);text-align:center}.right{margin-left:auto}.error{color:#cb3656}
@media(min-width:1600px){main{grid-template-columns:240px 400px minmax(600px,1fr)}}@media(max-width:1050px){main{grid-template-columns:190px 270px minmax(340px,1fr)}header input[type=search]{width:220px}}@media(max-width:800px){header{height:auto;flex-wrap:wrap;padding:12px}header input[type=search]{margin-left:0;flex:1}main{display:block;height:auto}aside{max-height:180px}#tree{max-height:380px}#detail{min-height:500px}}
</style></head><body>
<header><strong>Agent Traces</strong><span id="status" class="muted" role="status"></span><input id="search" type="search" placeholder="搜索会话、消息或文件" aria-label="搜索会话、消息或文件"><input id="date" type="date" aria-label="按日期筛选"><button id="refresh">刷新</button></header>
<main><aside><h3>会话 Sessions</h3><div id="scope" class="notice"></div><div id="sessions"></div></aside><section id="tree" aria-label="调用树"></section><article id="detail" aria-label="调用详情"><div class="empty">选择模型或工具调用，查看实际输入与输出。</div></article></main>
<script>
const $=id=>document.getElementById(id),el=(tag,text,cls)=>{const n=document.createElement(tag);if(text!=null)n.textContent=String(text);if(cls)n.className=cls;return n;};
let traces=[],sessionId='',mode='tree',selection=null,refreshing=false,generation=0,detailView=0;const cache=new Map(),cacheKeys=new Map();
const labels={SUCCEEDED:'完成',REPLY_PENDING:'待发送',RUNNING:'运行中',FAILED:'失败',CANCELLED:'已取消',QUEUED:'排队中',succeeded:'成功',failed:'失败',running:'运行中',incomplete:'未完整记录'};
const statusClass=s=>['SUCCEEDED','succeeded'].includes(s)?'succeeded':['FAILED','failed','DEAD_LETTER'].includes(s)?'failed':['RUNNING','REPLY_PENDING','QUEUED','running'].includes(s)?'running':'incomplete';
const badge=s=>el('span',labels[s]||s,'badge '+statusClass(s));
const time=v=>v==null?'—':new Date(v).toLocaleString('zh-CN',{hour12:false});
const duration=s=>s.start!=null&&s.end!=null?Math.max(0,s.end-s.start):null;
const ms=n=>n==null?'—':n<1000?Math.round(n)+' ms':(n/1000).toFixed(2)+' s';
const stamp=t=>new Date(t.queuedAt||t.capturedAt).getTime();
const title=t=>t.userPrompt||String(t.finalResponse||'文件消息').split('\n')[0];
const tokens=u=>u?(u.totalTokens??((u.input||0)+(u.output||0)+(u.cacheRead||0)+(u.cacheWrite||0))):null;
function raw(v){return el('pre',typeof v==='string'?v:JSON.stringify(v??null,null,2));}
function readableText(text){try{const parsed=JSON.parse(text);if(parsed&&typeof parsed==='object')return raw(parsed);}catch{}return el('div',text,'prose');}
function fold(name,node){const d=el('details');d.append(el('summary',name),node);return d;}
function card(name,node){const n=el('section',null,'card');n.append(el('div',name,'card-title'));const b=el('div',null,'card-body');b.append(node);n.append(b);return n;}
function blocks(content){const box=el('div');if(content==null){box.append(el('div','未采集','muted'));return box;}if(typeof content==='string'){box.append(readableText(content));return box;}if(!Array.isArray(content)){box.append(raw(content));return box;}
 content.forEach(c=>{const part=el('div',null,'block');if(c.type==='thinking'){part.classList.add('reasoning');part.append(el('div','Reasoning · 接口返回','pill'),typeof c.thinking==='string'?el('div',c.thinking||'接口未返回可显示的 reasoning 文本','prose'):raw(c));}
 else if(c.type==='reasoning'){part.append(reasoningView(c));}
 else if(c.type==='text'||c.type==='input_text'||c.type==='output_text'){part.append(typeof c.text==='string'?readableText(c.text):raw(c.text));}
 else if(c.type==='toolCall'){part.append(el('div','Tool Call · '+c.name,'pill'),raw({id:c.id,arguments:c.arguments}));}
 else if(c.type==='image'||c.type==='input_image'){part.append(el('div','图片 · '+(c.mimeType||'image')+'（二进制未展示）','notice'),fold('图片元信息',raw(c)));}
 else if(c.type==='input_file'){part.append(el('div','文件输入（签名及二进制已脱敏）','notice'),raw(c));}else part.append(raw(c));box.append(part);});return box;
}
function reasoningView(item){const box=el('div');let readable=false;
 for(const [label,parts]of [['Reasoning summary',item.summary],['Reasoning text',item.content]]){if(!Array.isArray(parts))continue;const textParts=parts.filter(part=>typeof part.text==='string'?part.text.trim().length>0:part.text?.truncated);if(!textParts.length)continue;readable=true;const section=el('div',null,'reasoning');section.append(el('div',label,'pill'));textParts.forEach(part=>section.append(typeof part.text==='string'?el('div',part.text,'prose'):raw(part.text)));box.append(section);}
 if(!readable)box.append(el('div',item.encrypted_content?'推理状态已回传，无可读摘要。':'接口未返回可读推理文本。','notice'));
 else if(item.encrypted_content)box.append(el('div','另有加密推理状态，已隐藏。','muted'));
 return box;
}
function messages(context){const box=el('div');if(!context){box.append(el('div','此记录未采集上下文','notice'));return box;}if(context.systemPrompt!==undefined)box.append(fold('System Prompt',raw(context.systemPrompt)));
 const list=context.messages||[];if(!Array.isArray(list)){box.append(raw(list));return box;}
 list.forEach((m,i)=>{const role=m.role==='toolResult'?'Tool Result · '+(m.toolName||''):m.role==='custom'?'Application Context':m.role||m.type||'Message';const body=m.type==='reasoning'?reasoningView(m):blocks(m.content??m);if(m.toolCallId)body.append(el('div','toolCallId: '+m.toolCallId,'muted'));box.append(card(String(i+1).padStart(2,'0')+' · '+role,body));});
 if(context.tools)box.append(fold('本次可用工具定义',raw(context.tools)));return box;
}
function output(v){const box=el('div');if(!v){box.append(el('div','尚无输出或未采集','notice'));return box;}if(Array.isArray(v.content)){box.append(card(v.role==='assistant'?'Assistant':'Tool Output',blocks(v.content)));if(v.role==='assistant'&&!v.content.some(c=>c.type==='thinking'&&c.thinking))box.append(el('div','接口未返回可展示的 reasoning 文本；不推测隐藏思考。','muted'));if(v.errorMessage)box.append(card('Error',el('div',v.errorMessage,'prose error')));if(v.details)box.append(fold('Details',raw(v.details)));return box;}return raw(v);}
async function get(url){const r=await fetch(url);if(!r.ok)throw Error('读取失败（HTTP '+r.status+'）');return r.json();}
function groups(){const q=$('search').value.trim().toLowerCase(),day=$('date').value,map=new Map();traces.forEach(t=>{const id=t.sessionId||t.turnId;if(!map.has(id))map.set(id,[]);map.get(id).push(t);});return [...map].map(([id,turns])=>({id,turns:turns.sort((a,b)=>stamp(a)-stamp(b))})).filter(s=>(!q||s.id.includes(q)||s.turns.some(t=>(title(t)+' '+t.turnId).toLowerCase().includes(q)))&&(!day||s.turns.some(t=>{const d=new Date(stamp(t));return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')===day;}))).sort((a,b)=>stamp(b.turns.at(-1))-stamp(a.turns.at(-1)));}
function renderSessions(){const all=groups();$('scope').textContent='最近 '+traces.length+' 条执行记录；超出保留范围的上下文快照可能不可用。';if(!all.some(s=>s.id===sessionId)){sessionId=all[0]?.id||'';selection=null;}$('sessions').replaceChildren();all.forEach(s=>{const b=el('button',null,'session'+(s.id===sessionId?' active':''));b.append(el('strong',title(s.turns[0]),'ellipsis'),el('div',s.turns.length+' turns · '+time(s.turns.at(-1).capturedAt),'muted'));b.onclick=()=>{sessionId=s.id;selection=null;renderSessions();void renderTree();};$('sessions').append(b);});}
function select(turn,span,payload){detailView=0;selection={turnId:turn.turnId,spanId:span?.id??null};$('tree').querySelectorAll('.selected').forEach(n=>n.classList.remove('selected'));const button=[...$('tree').querySelectorAll('[data-key]')].find(n=>n.dataset.key===turn.turnId+':'+(span?.id??'turn'));button?.classList.add('selected');inspect(turn,span,payload);}
function node(turn,span,payload,children,visited){if(visited.has(span.id))return el('div');const next=new Set(visited);next.add(span.id);const box=el('div');const b=el('button',null,'node');b.dataset.key=turn.turnId+':'+span.id;if(selection?.turnId===turn.turnId&&selection.spanId===span.id)b.classList.add('selected');const icon=span.kind==='model'?'◈':span.kind==='tool'?'⌘':span.kind==='context'?'↳':'◇';b.append(el('span',icon,'icon '+span.kind+'-icon'),el('span',span.name,'label ellipsis'),badge(span.status),el('span',ms(duration(span)),'muted'));b.title=span.name;b.onclick=()=>select(turn,span,payload);box.append(b);const nested=children.get(span.id)||[];if(nested.length){const wrap=el('div',null,'children');nested.forEach(s=>wrap.append(node(turn,s,payload,children,next)));box.append(wrap);}return box;}
async function renderTree(){const version=++generation;const group=groups().find(s=>s.id===sessionId);const pane=$('tree');pane.replaceChildren();if(!group){pane.append(el('div','没有匹配的会话','empty'));$('detail').replaceChildren();return;}
 pane.append(el('h3','调用结构'),el('div',sessionId,'muted ellipsis'));const tabs=el('div',null,'tabs');for(const [id,name]of [['tree','Tree'],['chat','Chat']]){const b=el('button',name,mode===id?'active':'');b.onclick=()=>{mode=id;void renderTree();};tabs.append(b);}pane.append(tabs);
 let initial=null;
 for(const [i,t]of group.turns.entries()){
  const shell=el('div',null,'turn');const head=el('button',null,'turn-head');head.dataset.key=t.turnId+':turn';head.append(el('strong','Turn '+String(i+1).padStart(2,'0')+' · '+title(t),'ellipsis'),el('div',time(t.queuedAt||t.capturedAt),'muted'),badge(t.status));shell.append(head);pane.append(shell);
  try{const key=JSON.stringify([t.status,t.capturedAt,t.completedAt]);const p=t.status!=='RUNNING'&&cacheKeys.get(t.turnId)===key?cache.get(t.turnId):await get('/debug/traces/'+encodeURIComponent(t.turnId));if(version!==generation)return;cache.set(t.turnId,p);cacheKeys.set(t.turnId,key);head.onclick=()=>select(t,null,p);const spans=p.spans||[];if(mode==='tree'){const children=new Map(),ids=new Set(spans.map(s=>s.id));spans.forEach(s=>{const parent=s.parentId&&ids.has(s.parentId)?s.parentId:null;if(!children.has(parent))children.set(parent,[]);children.get(parent).push(s);});const list=el('div',null,'nodes');(children.get(null)||[]).forEach(s=>list.append(node(t,s,p,children,new Set())));if(!spans.length)list.append(el('div','此记录没有调用详情','notice'));shell.append(list);}else{shell.append(card('User',el('div',t.userPrompt||'[附件消息]','prose')),card('Assistant / Application',el('div',t.finalResponse||t.errorMessage||'尚无回复','prose')));spans.filter(s=>s.kind==='model'||s.kind==='context').forEach(s=>{const b=el('button',s.name,'node');b.onclick=()=>select(t,s,p);shell.append(b);});}
   if(selection?.turnId===t.turnId){const s=spans.find(s=>s.id===selection.spanId);inspect(t,s??null,p);}initial={turn:t,span:spans.filter(s=>s.kind==='model').at(-1)||spans.at(-1)||null,payload:p};
  }catch(e){shell.append(el('div',e.message,'notice error'));}
 }
 if(!selection&&initial)select(initial.turn,initial.span,initial.payload);
}
function inspect(turn,span,p){const panel=$('detail');panel.replaceChildren();panel.append(el('h1',span?.name||'Turn · '+title(turn)));const meta=el('div',null,'meta');meta.append(badge(span?.status||turn.status),el('span','Latency: '+ms(span?duration(span):turn.completedAt&&turn.startedAt?new Date(turn.completedAt)-new Date(turn.startedAt):null)));if(span?.usage){meta.append(el('span','Tokens: '+tokens(span.usage).toLocaleString()));}meta.append(el('span',time(span?.start||turn.queuedAt)),el('span',turn.turnId,'ellipsis'));panel.append(meta);
 const tabs=el('div',null,'tabs'),body=el('div');const views=span?.kind==='model'?['运行','请求 JSON','原始事件']:['运行','JSON','原始事件'];views.forEach((name,i)=>{const b=el('button',name,i===detailView?'active':'');b.onclick=()=>{detailView=i;tabs.querySelectorAll('button').forEach(n=>n.classList.remove('active'));b.classList.add('active');body.replaceChildren();if(i===0)renderRun(body,turn,span,p);else if(i===1)body.append(raw(span?.kind==='model'?span.input?.providerRequest:span||p));else body.append(raw(span?.events||p.details?.steps));};tabs.append(b);});const copy=el('button','复制详情','right');copy.onclick=()=>{navigator.clipboard.writeText(JSON.stringify(span||p,null,2)).then(()=>{copy.textContent='已复制';}).catch(()=>{copy.textContent='复制失败';});};tabs.append(copy);panel.append(tabs,body);if(detailView===0)renderRun(body,turn,span,p);else if(detailView===1)body.append(raw(span?.kind==='model'?span.input?.providerRequest:span||p));else body.append(raw(span?.events||p.details?.steps));}
function renderRun(box,t,s,p){
 if(!s){box.append(el('h2','Input'),card('User',el('div',t.userPrompt||'[附件消息]','prose')));if(p.details?.message?.files)box.append(card('Files',raw(p.details.message.files)));if(p.details?.message?.images)box.append(card('Images',raw(p.details.message.images)));box.append(el('h2','Output'),card('Assistant / Application',el('div',t.finalResponse||t.errorMessage||'尚无回复','prose')));if(p.details?.outbox?.length)box.append(fold('消息发送状态',raw(p.details.outbox.map(o=>({chunkIndex:o.chunkIndex,status:o.status,lastError:o.lastError})))));const count=(p.spans||[]).filter(x=>x.kind==='model').length;box.append(el('div',count?'本轮包含 '+count+' 次模型调用，选择左侧模型节点查看每次实际上下文。':p.trace?.provider?'旧记录未采集模型调用快照，不能重建当时完整上下文。':'本轮由应用直接处理，没有调用模型。','notice'));return;}
 if(s.kind==='model'){const request=s.input?.providerRequest;const actual=request?(Array.isArray(request.input)?{systemPrompt:request.instructions,messages:request.input.map(m=>m.content?m:{...m,content:m.output??m}),tools:request.tools}:Array.isArray(request.messages)?{systemPrompt:request.system,messages:request.messages,tools:request.tools}:null):null;box.append(el('h2','Input · 实际发送上下文'),actual?messages(actual):messages(s.input?.context));box.append(fold('Agent 组织的上下文（协议转换前）',messages(s.input?.context)),fold('实际 Provider 请求（文件注入后，已脱敏）',raw(request)));if(!s.input?.providerRequest)box.append(el('div','尚未生成或未采集实际请求；请检查关联的文件/错误事件。','notice'));box.append(el('h2','Output'),output(s.output));if(s.usage)box.append(fold('Token / Usage',raw(s.usage)));return;}
 if(s.kind==='context'){box.append(el('div','此节点只更新会话上下文，不发起模型请求。恢复节点用于重启或压缩后的补入。','notice'),el('h2','Input · 会话事件'),raw(s.input),el('h2','Output · 更新后的上下文'),s.output?.messages?messages(s.output):raw(s.output));return;}
 box.append(el('h2','Input'),raw(s.input),el('h2','Output'),output(s.output));}
async function refresh(){if(refreshing)return;refreshing=true;try{const next=await get('/debug/traces?limit=100');const changed=JSON.stringify(next)!==JSON.stringify(traces);traces=next;const top=$('detail').scrollTop;renderSessions();if(changed||traces.some(t=>t.status==='RUNNING')||!$('tree').querySelector('.turn')){await renderTree();$('detail').scrollTop=top;}$('status').textContent='已更新 '+new Date().toLocaleTimeString();}catch(e){$('status').textContent=e.message;}finally{refreshing=false;}}
$('refresh').onclick=()=>void refresh();$('search').oninput=()=>{selection=null;renderSessions();void renderTree();};$('date').onchange=$('search').oninput;void refresh();setInterval(()=>{if(!document.hidden)void refresh();},10000);
</script></body></html>`;
