export const TRACE_PAGE_HTML = String.raw`<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Agent Traces</title>
<style>
:root{color-scheme:light;--bg:#f8fafc;--line:#e2e6ef;--muted:#697386;--ink:#202a3c;--accent:#5046e5}*{box-sizing:border-box}body{margin:0;font:14px/1.55 system-ui,-apple-system,sans-serif;color:var(--ink);background:var(--bg)}button,input{font:inherit}button{cursor:pointer;color:inherit;background:white;border:1px solid var(--line);border-radius:6px;padding:5px 10px}button:hover{background:#f0efff}button:focus-visible,input:focus-visible,summary:focus-visible{outline:2px solid var(--accent);outline-offset:2px}header{height:74px;display:flex;align-items:center;gap:16px;padding:16px 24px;border-bottom:1px solid var(--line);background:white}header strong{font-size:23px;white-space:nowrap}header input[type=search]{margin-left:auto;width:340px}input{border:1px solid var(--line);padding:8px 10px;border-radius:6px;min-width:0}main{display:grid;grid-template-columns:250px minmax(420px,1fr) 370px;height:calc(100vh - 74px)}aside,article,#inspector{overflow:auto;padding:20px;border-right:1px solid var(--line)}aside,#inspector{background:white}#inspector:empty:before{content:"选择一个调用，查看输入、输出与关联事件";display:block;color:var(--muted);margin:40px 0;text-align:center}h1{font-size:22px;margin:0 0 4px}h2{font-size:16px;margin:0 0 10px}h3{font-size:13px;margin:0 0 8px}.muted{color:var(--muted);font-size:12px}.session{display:block;width:100%;text-align:left;padding:14px 12px;margin:7px 0;border:1px solid transparent;background:transparent}.session.active{background:#eeedff;border-color:#dad7ff}.ellipsis{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.turn{border:1px solid var(--line);border-radius:9px;background:white;margin:14px 0;overflow:hidden}.turn[open]{border-color:#a9a3ff}.turn>summary{padding:13px;cursor:pointer;background:#fcfcff}.turn-title{display:inline-flex;width:calc(100% - 22px);align-items:center;gap:9px;vertical-align:middle}.turn-title .ellipsis{flex:1}.badge{display:inline-block;border:1px solid currentColor;border-radius:5px;font-size:11px;padding:1px 6px;white-space:nowrap}.succeeded{color:#168151}.failed{color:#c43649}.running{color:#b86d00}.incomplete{color:#697386}.section{padding:16px;border-top:1px solid var(--line)}.section>h3{color:var(--accent);font-size:11px;letter-spacing:.06em}.prose{white-space:pre-wrap;overflow-wrap:anywhere}.toolbar{display:flex;gap:8px;align-items:center;margin:12px 0;flex-wrap:wrap}.span{border-top:1px solid #edf0f6;padding:8px 0}.span>summary{cursor:pointer;display:flex;align-items:center;gap:8px;list-style:none}.span>summary:before{content:'›';color:var(--muted)}.span[open]>summary:before{content:'⌄'}.span.selected{background:#f2f0ff}.span-name{flex:1;min-width:0;text-align:left;border:0;background:transparent;padding:2px}.waterfall{position:relative;width:30%;height:18px;background:repeating-linear-gradient(to right,#e9ecf4 0px,#e9ecf4 1px,transparent 1px,transparent 25%);flex-shrink:0}.bar{position:absolute;top:4px;height:10px;min-width:3px;background:#8c84f0;border-radius:2px}.bar.failed{background:#dc6575}.bar.running{background:#e5ab48}.duration{font-size:11px;min-width:56px;text-align:right;color:var(--muted)}.span-preview{padding:12px 16px;background:#fafaff}.tabs{display:flex;gap:8px;border-bottom:1px solid var(--line);margin:20px 0 12px}.tabs button{border:0;border-radius:0;padding:8px}.tabs button.active{color:var(--accent);border-bottom:2px solid var(--accent)}.field{border:1px solid var(--line);border-radius:7px;padding:10px;margin:8px 0;overflow-wrap:anywhere}.field>summary{cursor:pointer}.field-label{font-size:12px;color:var(--muted);margin-bottom:5px}pre{font:12px/1.6 ui-monospace,monospace;white-space:pre-wrap;overflow-wrap:anywhere;margin:0;max-height:430px;overflow:auto}.empty{padding:40px 10px;text-align:center;color:var(--muted)}.error{padding:12px;background:#fff1f2;color:#b8283d;border-radius:6px}.inspector-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.meta-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:18px 0}.notice{padding:8px 10px;background:#f5f6fa;border-radius:5px;color:var(--muted);font-size:12px}.link{color:var(--accent)}.title-row{display:flex;justify-content:space-between;gap:12px}.out{background:#f9fcfa}.axis{display:flex;justify-content:space-between;margin-left:70%;font-size:10px;color:var(--muted)}#refresh-status{max-width:180px} @media(min-width:1650px){main{grid-template-columns:280px minmax(500px,1fr) 440px}}@media(max-width:1100px){main{grid-template-columns:210px minmax(0,1fr)}#inspector{position:fixed;right:0;top:74px;bottom:0;width:380px;box-shadow:-8px 0 30px #202a3c20;z-index:5}#inspector:empty{display:none}header{gap:8px;padding:12px}header input[type=search]{width:220px}}@media(max-width:700px){header{height:auto;flex-wrap:wrap}header strong{font-size:19px}header input[type=search]{order:3;width:100%}main{height:auto;display:block}aside{max-height:230px;border-bottom:1px solid var(--line)}article{padding:12px}#inspector{top:0;width:100%;z-index:10}.waterfall{width:23%}.turn-title{gap:5px}.turn-title .muted{display:none}}
</style></head><body>
<header><strong>Agent Traces</strong><span id="refresh-status" class="muted" role="status"></span><input id="search" type="search" aria-label="搜索会话、消息或 Turn ID" placeholder="搜索会话、消息或 Turn ID"><input id="date" type="date" aria-label="按日期筛选"><button id="refresh">刷新</button></header>
<main><aside><h2>会话 <span class="muted">Sessions</span></h2><div id="scope" class="notice"></div><div id="sessions"></div></aside><article id="turns"><div class="empty">正在加载会话…</div></article><section id="inspector" aria-label="调用详情"></section></main>
<script>
const $=id=>document.getElementById(id);
const el=(tag,value,cls)=>{const n=document.createElement(tag);if(value!=null)n.textContent=String(value);if(cls)n.className=cls;return n;};
let traces=[],selectedSession='',generation=0,refreshing=false,focused=null;
const spanOpened=new Set();
const opened=new Set(),cache=new Map();
const labels={SUCCEEDED:'完成',REPLY_PENDING:'待发送',RUNNING:'进行中',FAILED:'失败',DEAD_LETTER:'发送失败',CANCELLED:'已取消',QUEUED:'排队中',succeeded:'成功',failed:'失败',running:'运行中',incomplete:'未完整记录'};
const statusClass=s=>['SUCCEEDED','succeeded'].includes(s)?'succeeded':['FAILED','DEAD_LETTER','failed'].includes(s)?'failed':['RUNNING','QUEUED','REPLY_PENDING','running'].includes(s)?'running':'incomplete';
const badge=s=>el('span',labels[s]||s,'badge '+statusClass(s));
const time=v=>v!=null?new Date(v).toLocaleString('zh-CN',{hour12:false}):'—';
const ms=v=>v==null||v<0?'—':v<1000?Math.round(v)+' ms':(v/1000).toFixed(2)+' s';
const stamp=t=>new Date(t.queuedAt||t.capturedAt).getTime();
const localDate=v=>{const d=new Date(v);return [d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-');};
async function json(url){const r=await fetch(url);if(!r.ok)throw Error(r.status===404?'该 Trace 已不存在或超出保留范围':'读取失败（HTTP '+r.status+'）');return r.json();}
function safeRun(fn){return ()=>{Promise.resolve().then(fn).catch(e=>{$('refresh-status').textContent=e.message;});};}
function disclosure(title,body){const d=el('details',null,'field');d.append(el('summary',title),body);return d;}
function valueNode(value,depth=0){
 if(value==null)return el('div','未采集','muted');
 if(typeof value!=='object')return el('div',value,'prose');
 const box=el('div');if(depth>8){box.append(el('pre',JSON.stringify(value,null,2)));return box;}
 if(value.truncated)box.append(el('div','内容已截断，仅展示已保存的部分','notice'));
 Object.entries(value).forEach(([key,v])=>{const field=el('div',null,'field');field.append(el('div',key,'field-label'));if(v&&typeof v==='object')field.append(disclosure(Array.isArray(v)?v.length+' 项':'展开字段',valueNode(v,depth+1)));else field.append(valueNode(v,depth+1));box.append(field);});return box;
}
function resultNode(v){if(v&&Array.isArray(v.content)){const box=el('div');v.content.forEach(c=>box.append(c.type==='text'?el('pre',c.text):valueNode(c)));if(v.details)box.append(disclosure('附加字段',valueNode(v.details)));return box;}return valueNode(v);}
function sessions(){const q=$('search').value.trim().toLowerCase(),date=$('date').value;const map=new Map();traces.forEach(t=>{const id=t.sessionId||t.turnId;if(!map.has(id))map.set(id,[]);map.get(id).push(t);});return [...map].map(([id,turns])=>({id,turns:turns.sort((a,b)=>stamp(a)-stamp(b)||a.turnId.localeCompare(b.turnId))})).filter(s=>(!q||s.id.toLowerCase().includes(q)||s.turns.some(t=>(t.userPrompt+' '+t.turnId).toLowerCase().includes(q)))&&(!date||s.turns.some(t=>localDate(t.queuedAt||t.capturedAt)===date))).sort((a,b)=>Math.max(...b.turns.map(stamp))-Math.max(...a.turns.map(stamp)));}
function renderSessions(){const groups=sessions();$('sessions').replaceChildren();$('scope').textContent='最近 '+traces.length+' 条已保留的执行记录 · 会话可能不完整';if(!groups.some(s=>s.id===selectedSession)){selectedSession=groups[0]?.id||'';focused=null;$('inspector').replaceChildren();}groups.forEach(s=>{const b=el('button',null,'session'+(s.id===selectedSession?' active':''));b.append(el('strong',s.turns[0].userPrompt||'文件会话','ellipsis'),el('div',s.turns.length+' turns · '+time(s.turns.at(-1).capturedAt),'muted'),el('div',s.id,'muted ellipsis'));if(s.turns.some(t=>statusClass(t.status)==='failed'))b.append(badge('failed'));b.onclick=()=>{selectedSession=s.id;focused=null;$('inspector').replaceChildren();renderSessions();void renderTurns();};$('sessions').append(b);});if(!groups.length)$('sessions').append(el('div','没有匹配的会话','empty'));}
async function renderTurns(){const version=++generation;const s=sessions().find(s=>s.id===selectedSession);const pane=$('turns');pane.replaceChildren();if(!s){pane.append(el('div','暂无 Trace，或没有符合条件的会话','empty'));return;}
 const head=el('div');head.append(el('h1',s.turns[0].userPrompt||'文件会话'),el('div','Session '+s.id+' · 按时间正序','muted'));pane.append(head);
 const help=el('div','展示保留范围内的 Agent 调用和文件接收；模型轮次正文未采集，原始事件可在上下文查看。','notice');pane.append(help);
 const actions=el('div',null,'toolbar');const latest=el('button','跳到最新');latest.onclick=()=>{const last=pane.querySelector('.turn:last-child');if(last){last.open=true;last.scrollIntoView({block:'start',behavior:'smooth'});}};actions.append(latest);for(const [label,open]of [['展开全部',true],['收起全部',false]]){const b=el('button',label);b.onclick=()=>{s.turns.forEach(t=>open?opened.add(t.turnId):opened.delete(t.turnId));pane.querySelectorAll('.turn').forEach(n=>n.open=open);};actions.append(b);}pane.append(actions);
 if(!s.turns.some(t=>opened.has(t.turnId))&&!cache.has('visited:'+s.id)){opened.add(s.turns.at(-1).turnId);cache.set('visited:'+s.id,true);}
 for(const [index,t]of s.turns.entries()){
  const d=el('details',null,'turn');d.dataset.id=t.turnId;const summary=el('summary');const line=el('span',null,'turn-title');line.append(el('strong','Turn '+String(index+1).padStart(2,'0')),el('span',time(t.queuedAt||t.capturedAt),'muted'),el('span',t.userPrompt||'用户发送文件','ellipsis'),badge(t.status),el('span',t.completedAt&&t.startedAt?ms(new Date(t.completedAt)-new Date(t.startedAt)):'—','duration'));summary.append(line);d.append(summary);const content=el('div');d.append(content);let loaded=false;
  const load=async()=>{if(!d.open||loaded)return;loaded=true;content.replaceChildren(el('div','加载执行链路…','section muted'));try{const p=await json('/debug/traces/'+encodeURIComponent(t.turnId));if(version!==generation)return;cache.set(t.turnId,p);renderTurn(content,t,p); }catch(e){loaded=false;content.replaceChildren(el('div',e.message,'section error'));}};
  d.ontoggle=()=>{if(d.open)opened.add(t.turnId);else opened.delete(t.turnId);void load();};pane.append(d);d.open=opened.has(t.turnId);if(d.open)void load();
 }
}
function section(title,body,cls=''){const n=el('section',null,'section '+cls);n.append(el('h3',title),body);return n;}
function renderTurn(container,t,p){container.replaceChildren();const details=p.details||{},trace=p.trace;container.append(section('INPUT',el('div',trace.userPrompt||'用户发送文件','prose')));if(details.message?.images?.length)container.append(section('图片附件',valueNode(details.message.images)));if(details.message?.files?.length)container.append(section('文件附件',el('div',details.message.files.map(f=>f.name+(f.declaredBytes?' · '+f.declaredBytes+' bytes':'')).join(String.fromCharCode(10)),'prose')));
 const chain=el('div');const toolbar=el('div',null,'toolbar');toolbar.append(el('h2','执行链路'));const check=el('input');check.type='checkbox';const label=el('label');label.append(check,document.createTextNode(' 只看异常'));toolbar.append(label);chain.append(toolbar);
 const spans=p.spans||[];const start=new Date(t.startedAt||t.capturedAt).getTime();const end=Math.max(start+1,...spans.map(s=>s.end||s.start||start),t.completedAt?new Date(t.completedAt).getTime():start);const total=end-start;const axis=el('div',null,'axis');axis.append(el('span','0 s'),el('span',ms(total/2)),el('span',ms(total)));chain.append(axis);
 const rows=el('div');chain.append(rows);const draw=()=>{rows.replaceChildren();const visible=spans.filter(s=>!check.checked||['failed','incomplete'].includes(s.status));if(!visible.length)rows.append(el('div',check.checked?'没有异常调用':'未采集到工具或 Skill 事件','notice'));
 visible.forEach(s=>{const row=el('details',null,'span');const spanKey=t.turnId+':'+s.id;row.open=spanOpened.has(spanKey)||s.status==='failed';row.ontoggle=()=>{if(row.open)spanOpened.add(spanKey);else spanOpened.delete(spanKey);};const summary=el('summary');const args=s.input||{};const hint=args.path||args.filePath||args.command||args.url||'';const name=el('button',(s.kind==='skill'?'◇ Skill · ':s.kind==='tool'?'⌘ ':s.kind==='file'?'▤ 文件 · ':'↻ ')+s.name+(hint?' · '+String(hint).slice(0,75):''),'span-name ellipsis');name.title='查看输入、输出与关联事件';name.onclick=e=>{e.preventDefault();rows.querySelectorAll('.selected').forEach(n=>n.classList.remove('selected'));row.classList.add('selected');inspect(s,t,p);};summary.append(name,badge(s.status),el('span',s.start!==null&&s.end!==null?ms(s.end-s.start):'—','duration'));
 const lane=el('span',null,'waterfall');const bar=el('span',null,'bar '+s.status);const offset=s.start??s.end;if(offset!==null){bar.style.left=Math.max(0,Math.min(99,(offset-start)/total*100))+'%';bar.style.width=Math.max(.5,Math.min(100,((s.end??offset)-offset)/total*100))+'%';lane.append(bar);}summary.append(lane);row.append(summary);const preview=el('div',null,'span-preview');preview.append(el('div',s.start===null?'缺少开始事件，耗时未知':s.end===null?'尚无结束事件':'开始 '+time(s.start)+' → 结束 '+time(s.end),'muted'));preview.append(el('h3','输入'),valueNode(s.input),el('h3','输出'),resultNode(s.output));row.append(preview);rows.append(row);});};check.onchange=draw;draw();container.append(section('TRACE',chain));
 container.append(section('OUTPUT',el('div',trace.finalResponse||trace.errorMessage||(t.status==='RUNNING'?'正在生成回复…':'没有最终回复'),'prose'),'out'));
 const deliveries=details.outbox||[],deliveryBox=el('div');deliveries.forEach(o=>{const item=el('div',null,'field');item.append(el('strong','消息片段 '+(Number(o.chunkIndex)+1)+' · '+({SENT:'已发送',PENDING:'待发送',SENDING:'发送中',FAILED:'失败',DEAD_LETTER:'发送失败'}[o.status]||o.status)));if(o.lastError)item.append(el('div',o.lastError,'error'));deliveryBox.append(item);});container.append(section('消息发送',deliveries.length?deliveryBox:el('div','尚无发送记录','muted')));
 const context=el('div');context.append(el('div',t.turnId+' · '+(trace.provider?trace.provider+' / '+trace.modelId:'未调用模型'),'muted'),disclosure('System Prompt',el('pre',trace.systemPrompt)),disclosure('可用 Skills / Tools',valueNode({skills:trace.skills,tools:trace.tools})),disclosure('Skill Loads',valueNode((details.steps||[]).filter(e=>e.event_type==='skill_load').map(e=>e.eventData))),disclosure('原始事件（含模型与流式事件）',el('pre',JSON.stringify(details.steps||[],null,2))));container.append(section('上下文',context));if(focused?.turnId===t.turnId){const span=spans.find(s=>s.id===focused.id);if(span)inspect(span,t,p);}
}
function inspect(s,t,p){focused={turnId:t.turnId,id:s.id};const panel=$('inspector');panel.replaceChildren();const head=el('div',null,'inspector-head');head.append(el('h1',s.name),badge(s.status));const close=el('button','关闭');close.onclick=()=>{focused=null;panel.replaceChildren();};head.append(close);panel.append(head,el('div',t.turnId+' · '+s.id,'muted prose'));const meta=el('div',null,'meta-grid');for(const [key,v]of [['开始',time(s.start)],['结束',time(s.end)],['耗时',s.start!==null&&s.end!==null?ms(s.end-s.start):'未知'],['关联事件',s.events.length]]){const n=el('div');n.append(el('div',key,'muted'),el('div',v));meta.append(n);}panel.append(meta);const tabs=el('div',null,'tabs'),body=el('div');for(const [i,title]of ['输入','输出','原始事件'].entries()){const b=el('button',title,i===0?'active':'');b.onclick=()=>{tabs.querySelectorAll('button').forEach(n=>n.classList.remove('active'));b.classList.add('active');body.replaceChildren(i===0?valueNode(s.input):i===1?resultNode(s.output):el('pre',JSON.stringify(s.events,null,2)));};tabs.append(b);}panel.append(tabs,body);body.append(valueNode(s.input));const copy=el('button','复制调用详情');copy.onclick=safeRun(async()=>{await navigator.clipboard.writeText(JSON.stringify(s,null,2));copy.textContent='已复制';});panel.append(copy,disclosure('上下文与 System Prompt',el('pre',p.trace.systemPrompt)),el('div',s.kind==='tool'?'工具事件按本轮 toolCallId 配对；未采集的内容不推断。':s.kind==='file'?'文件阶段按事件 ID 配对；选择与附加是时间点事件。':'此项是时间点事件，不代表独立的执行耗时。','notice'));}
async function refresh(){if(refreshing)return;refreshing=true;try{const next=await json('/debug/traces?limit=100');const changed=JSON.stringify(next)!==JSON.stringify(traces);traces=next;renderSessions();if(changed||!$('turns').querySelector('.turn'))await renderTurns();else {const version=generation;for(const t of traces.filter(t=>t.sessionId===selectedSession&&t.status==='RUNNING'&&opened.has(t.turnId))){const p=await json('/debug/traces/'+encodeURIComponent(t.turnId));if(version!==generation)break;const turn=[...$('turns').querySelectorAll('.turn')].find(n=>n.dataset.id===t.turnId);if(turn&&turn.open)renderTurn(turn.lastElementChild,t,p);}}$('refresh-status').textContent='已更新 '+new Date().toLocaleTimeString();}catch(e){$('refresh-status').textContent=e.message;if(!traces.length)$('turns').replaceChildren(el('div',e.message+'，请点击刷新重试。','error'));}finally{refreshing=false;}}
$('refresh').onclick=safeRun(async()=>{await refresh();await renderTurns();});$('search').oninput=()=>{renderSessions();void renderTurns();};$('date').onchange=()=>{renderSessions();void renderTurns();};void refresh();setInterval(()=>{if(!document.hidden)void refresh();},10000);
</script></body></html>`;
